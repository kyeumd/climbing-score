# 진행 현황

마지막 갱신: 2026-08-31

## 한 줄 요약

서울 클라이밍장 110곳 데이터를 갖춘 정적 웹앱. 암장에서 친구들과 완등 개수로 점수를 겨룬다.
기능은 다 돌아가고 **UI 검토도 7차까지 끝났다.** 자동 검사 0건, 화면 캡처 80장 무실패,
페이지 E2E 24개 단정 통과. 남은 건 배포 결정과 문서 정리다.

## 실행

```bash
python3 tools/dev-server.py 8099     # no-cache 개발 서버
open http://localhost:8099/index.html
```

`tools/demo.html` 은 흐름 테스트 전용이다. **사람이 보는 화면은 항상 `index.html`.**
이걸 혼동해서 꾸민 데모 화면을 보며 판단한 적이 있다. 아래 "검토 방법" 참고.

---

## 끝난 것

### 입력 방식 — 난이도 × 참가자 격자

참가자를 전환하며 기록하던 방식을 버렸다. 3명이 각자 5개를 깨면 전환 6번에 탭 15번이었다.
격자로 두면 전환 없이 15번으로 끝난다.

- [x] 세로 난이도, 가로 참가자. 아무 칸이나 탭하면 그 사람의 그 난이도가 +1
- [x] **열 순서는 참가자 추가 순서로 고정.** 점수순으로 정렬하면 탭할 때마다 사람이 자리를 바꿔서
      누르려던 칸이 다른 사람 것이 된다. 순위는 이름 옆 배지로만 알린다 (동점은 같은 순위)
- [x] 열이 1~4명까지 정확히 잡히는 것을 확인 (1명 2열 ~ 4명 5열)
- [x] 기록이 없어도 열이 보인다 → "참가자를 추가했는데 화면에 안 나타난다" 문제도 함께 해소

### 점수

- [x] 숙련도 기반: `내 레벨(L)` 과 `문제 난이도(d)` 의 차이로 계산. 대각선(d = L)이 항상 기준 점수라
      색 체계가 다른 암장끼리도 스케일이 맞는다
- [x] **읽기 쉬운 숫자.** 기준을 10으로 낮추고 100 이상은 유효숫자 두 자리로 다듬는다

      이전  100  150  225  338  506  759  1139  1709  2563  3844  5767
      지금   10   15   23   34   51   76   110   170   260   380   580

      두 자리 이하는 그대로 둬서 낮은 난이도의 차이(10 vs 15)가 사라지지 않게 했다
- [x] 숙련도는 프로필당 하나 (짐별 아님). 짐을 옮긴다고 실력이 달라지지 않는다
- [x] 레벨을 바꾸면 **오늘 기록은 따라가고 지난 기록은 그대로**. 안 그러면 카드에 보이는 단가와
      합계 기준이 어긋난다
- [x] 점수표 편집 (배율 3개 + 개별 칸 수정). 배율에 0을 넣으면 모든 칸이 뭉개지므로 유효 범위로 가둔다

### 조작

- [x] 탭 +1, 길게 눌러 −1
- [x] 길게 누르기 **250ms**, 누른 채로 두면 200ms마다 연속 감소
- [x] 판정은 손 뗄 때 한다. 누르는 순간 화면을 다시 그리면 요소가 교체되어 손 뗄 때 새 요소가
      클릭을 받아 +1이 한 번 더 들어갔다

### 그 외 기능

- [x] 짐 선택기: 검색 하나로 이름과 지역을 함께 찾는다 (지역 드롭다운 제거). 자주 가는 곳(★)은 맨 위
- [x] 짐 설정 (난이도 색 추가·순서·이름·retire)
- [x] 프로필 다중 관리 + 숙련도 슬라이더
- [x] 통계 (누적 점수·세션 추이·단계별 완등·대결 전적)
- [x] 세션 편집 (날짜·그날의 레벨·개수). 합계와 저장 버튼은 sticky 푸터로 항상 노출
- [x] JSON 내보내기/가져오기

### 데이터

- [x] 서울 110곳 / 25개 구
- [x] 난이도 색 체계 98곳 (89%)
- [x] 색 등급 미사용 12곳 → **"지구력 중심"으로 표시.** 데이터 누락이 아니라 실제 성격이다
      (강동클라이밍짐=리드 교육센터, 권영세=테이프 번호 방식, 볼더룸=문보드·킬터보드,
       써니사이드=지구력 YDS 5.8~)
- [x] 폐업·비클라이밍장 3곳 제외 (브이업짐=헬스체인, 이창현=폐업, 몽키즈 마포=본사주소)
- [x] 별칭 병합 (서울볼더스 선유=클라이밍 컴퍼니, 온사이트=온플릭 삼성점 등)
- [x] **시드 버전 갱신.** 예전엔 최초 1회만 적재해서, 목록이 46곳에서 110곳으로 늘어도 이미 앱을
      켠 사람에게는 영원히 46곳이었다. 버전이 오르면 즐겨찾기·직접 고친 난이도·점수표·짐 id를
      지키면서 목록만 갱신한다

### 시각

- [x] 난이도를 **색 + 홀드 모양**으로 이중 부호화 (색각이상 대응). 실제 홀드 8종 실루엣
- [x] 홀드 외곽선을 **색 자체의 밝기로 결정.** 테마 대비색으로 고정하면 흰색은 라이트에서,
      검정은 다크에서 배경에 묻힌다 (실측 1.08:1)
- [x] **모바일 타이포.** 본문 15 → 17px, 보조 12 → 13px. iOS 기본이 17px인데 데스크톱 감각으로
      짜여 있었다
- [x] **회색을 걷어냄.** WCAG 4.5:1은 겨우 읽히는 최소선이라 실사용에서 안 보였다. 11:1 이상으로
- [x] 모달 스크롤 컨테이너를 1개로 (중첩 제거)
- [x] 목록이 있는 모달은 높이 고정. 검색어를 한 글자 칠 때마다 모달이 오르내리며 입력창이
      손가락 아래에서 도망갔다

### 검증

- [x] 단위 테스트 25개
- [x] 상호작용 흐름 7종 (`npm run flow`)
- [x] 모달 감사 14종, axe-core 접근성 위반 0건
- [x] **진짜 입력으로 검증.** CDP `Input` 도메인으로 실제 클릭과 타이핑을 보낸다.
      `el.value = '...'` + 합성 이벤트는 `isTrusted:false` 라서 브라우저 기본 동작을 거치지 않고
      실제 입력과 다르게 움직인다
- [x] 옛 데이터가 남은 상태를 재현하는 테스트 (`tools/upgrade-test.mjs`)

### 되돌린 것

- [x] **오프라인(PWA) 제거.** 1차 범위에서 뺀 항목인데 임의로 넣었고, 서비스워커 캐시가 모든
      수정을 가렸다. 이미 등록된 것을 지우기 위해 자기를 해제하는 `sw.js` 만 남겼다.
      모든 클라이언트가 최신 파일을 받은 뒤에는 그 파일도 지워도 된다

---

## 남은 것 (우선순위)

### P0 — 없음

5~7차 검토에서 UI 항목을 모두 처리했다. 자세한 내역은
[`docs/UI-REVIEW.md`](docs/UI-REVIEW.md).

- [x] 짐 선택기 목록 정렬 — 홀드 띠 폭을 고정해 이름 시작점을 맞췄다.
      색 등급이 없는 곳은 점선 자리 표시로 채운다
- [x] 홀드 미리보기 — 5개 + `+N` 배지. 좁은 화면에서는 크기를 줄여 다섯 개가 다 들어간다
      (예전에는 CSS 로 3개만 숨기면서 배지는 `전체 − 5` 라고 적어 거짓말을 했다)
- [x] 확인 배너 세로 공간 — 좁은 화면 전용 규칙으로 116px. 격자 3줄이 함께 보인다
- [x] 격자 셀 크기·간격 — 참가자 1~5명 x 두 뷰포트를 `shot-grid.mjs` 로 매번 찍는다
- [x] 통계·세션편집과의 시각적 일관성 — 6차에서 화면별로 점검

### P1 — 데이터: 더 캘 것이 없다 (2026-08-31 확인)

난이도 자료가 없는 12곳을 spiri7 API 로 다시 조회했다. **결론은 "자료가 없는 게 아니라
색 등급을 안 쓰는 곳"** 이다. 12곳 전부 `지구력`·`리드`·`문보드`·`킬터보드` 중심이다.

한 곳(북한산클라이밍센터)만 20단계가 돌아왔는데, 표준 V등급(Vb, V0-, V0, V0+, V1~V16)이었고
**전혀 다른 짐과 글자 하나까지 같은 목록**이었다. 색 체계를 등록하지 않은 짐에 API 가 돌려주는
기본값이지 그 짐의 벽 세팅이 아니다. 게다가 이름·주소가 우리 기록과 달라 같은 곳인지도 확실하지 않다.

- [x] `scrape-gyms.mjs` 가 기본 V등급 목록을 걸러낸다. 안 그러면 다음 수집 때
      12곳에 없는 데이터가 생긴다
- [x] 앱은 이미 이유를 정확히 말한다 — "○○은 지구력 중심으로 알려진 곳이라 색 등급 자료가 없어요"
- [ ] 남은 일은 현장 확인뿐이다. 실제로 색 등급을 쓰는 곳이 있으면 앱에서 직접 추가하면 된다

미등재 후보(맥클라이밍·블랙야크 야크돔·쿠키즈)는 4개 DB 어디에도 없어 그대로 둔다.

### P2 — 배포

- [x] 설계서 5차 개정 — 격자 입력·점수 스케일·짐 선택기 구조 반영
- [ ] GitHub Pages 배포 (**보류**)
- [ ] PWA/오프라인 (**보류** — 예전에 서비스워커가 모든 수정을 가린 적이 있다)

---

## 검토 방법 (여기서 실패를 많이 했다)

자동 검사가 "0건"이라고 해서 넘어갔다가 눈에 보이는 문제를 여러 번 놓쳤다.

| 원인 | 무슨 일이 벌어졌나 | 대책 |
|---|---|---|
| **데모 화면을 봤다** | `tools/demo.html`(꾸민 화면)로 판단. 사용자는 `index.html`을 봄 | `review.mjs`가 실제 앱만 캡처 |
| **축소해서 봤다** | 300px 썸네일 격자. 정렬·여백·경계선이 안 보임 | 원본 크기로 한 장씩 |
| **수치를 근거로 삼았다** | "테스트 통과, 검사 0건"으로 판단 | 판정은 항상 눈으로. 수치는 회귀 확인용 |
| **가짜 입력을 썼다** | `el.value` + 합성 이벤트. `isTrusted:false` | CDP `Input` 도메인으로 진짜 클릭·타이핑 |
| **성공 판정이 느슨했다** | 스크린샷만 찍고 "에러 안 남"을 통과로 봄 | 결과를 단정(assert)한다 |
| **localStorage를 매번 비웠다** | 사용자에게 남은 옛 데이터를 못 봄 | 옛 상태 재현 테스트 추가 |
| 캐시 (HTTP) | 서버가 `Cache-Control` 미전송 → 고쳐도 화면 그대로 | `tools/dev-server.py` (no-store) |
| 캐시 (서비스워커) | 내가 넣은 PWA가 모든 수정을 가림 | PWA 제거 + 해제용 `sw.js` |
| 브라우저 프로필 공유 | 두 번째 실행이 기존 브라우저에 붙어 옛 페이지 검사 | 실행마다 프로필·포트 분리 |
| 뷰포트 단일 | 414×896만 검사. 문제는 360×640에서만 | 뷰포트 3종 |
| 기하 추정 | `getBoundingClientRect`로 클리핑을 추정 | `document.elementFromPoint`로 브라우저에 질의 |

### CSS 함정 (같은 실수 반복 방지)

- `repeat(var(--cols), 1fr)` — 커스텀 속성이 정수로 해석되지 않아 `grid-template-columns`
  **선언 전체가 무효**가 된다. 열이 행으로 쌓여 화면이 무너진다
- `:has(> :nth-child(n))` — 구형 사파리 미지원
- → 렌더 시점에 아는 값이면 **인라인 style로 직접** 준다

```bash
npm run review          # 실제 앱 화면 캡처 -> tools/shots/dark/
npm run review:light
npm run review:small
npm run flow            # 상호작용 흐름 7종 스트립
npm test                # 단위 테스트
npm run scrape          # 암장 데이터 재수집
```

## 데이터 출처

- [스피릿(spiri7)](https://spiri7.com/) — 목록 + 난이도.
  `api.spiri7.com/api/v1/companies/gyms/{id}/skill?exerciseId=1` 의 `match_level` 이 난이도 순서다.
  `exercises` 에 볼더링이 없으면 색 등급을 안 쓰는 암장이다 (블로그 조사로 5곳 교차 확인)
- [climblife](https://climblife.co.kr/) — 목록 보강
- [OpenStreetMap](https://www.openstreetmap.org/copyright) — 목록 보강 (ODbL)

정부 공공데이터에는 클라이밍장 통계가 없다. 인공암벽장업이 체육시설업 등록 대상이 아니다.
